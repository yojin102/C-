using System;
using System.Collections.Generic;
using System.Diagnostics.SymbolStore;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace FirstProject
{
    /* class TV
    {
        // TV 속성(브랜드, 모델, 화면크기, 해상도, 디스플레이 유형 ..)
        string brand;       // 브랜드
        string model;       // 모델명
        int displaySize;    // 화면 크기
        string resoulution; // 해상도
        string displayType; // 디스플레이 유형

        // TV 기능(스마트기능, 음성제어, 앱지원, 화면공유...)
        // 스마트 기능(인터넷 연결하기, 스트리밍 서비스 받기...)
        bool InternetConnect()  // 인터넷 연결하기 기능
        {
            bool onOff;
            return onOff;
        }
        void StreamingService() { } // 스트리밍서비스받기 기능
        void


    }
    internal class ex1026   // 파일 이름으로 클래스를 정의
    {
        static void Main(string[] args)
        {
            // 변수 선언, 값 할당
            int a = 10; // a라는 이름으로 정수형 변수 선언. 값(10)을 할당
            
            // 클래스에서 객체를 생성
            // 클래스명 객체변수이름 = new 클래스명();
            TV naYeonTV = new TV();
            naYeonTV.brand = "삼성";
            naYeonTV.displaySize = 43;
            naYeonTV.InternetConnect();
        }
    }*/
}
